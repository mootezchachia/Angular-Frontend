/*import 'package:flutter/material.dart';
import 'package:vip/components/text_field_container.dart';
import 'package:vip/constants.dart';

class RoundedLicenseField extends StatelessWidget {
  final ValueChanged<String> onChanged;
  const RoundedLicenseField({
    required Key key,
    required this.onChanged,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextFieldContainer(
      child: TextField(
        obscureText: true,
        onChanged: onChanged,
        cursorColor: kPrimaryColor,
        decoration: InputDecoration(
          hintText: "Your Car License No ",
          icon: Icon(
            Icons.lock,
            color: kPrimaryColor,
          ),
          suffixIcon: Icon(
            Icons.visibility,
            color: kPrimaryColor,
          ),
          border: InputBorder.none,
        ),
      ),
    );
  }
}
*/
